<?xml version="1.0" encoding="utf-8"?>
<rss version="2.0" xml:base="{{ blog["baseurl"] }}"  xmlns:dc="http://purl.org/dc/elements/1.1/">
<channel>
    <title>{{ blog["title"] }}</title>
    <link>{{ blog["baseurl"] }}</link>
    <description>{{ blog["description"] }}</description>
    <language>{{ default_language }}</language>
    {% for post in posts %}
    <item>
        <title>{{ post["title"] }}</title>
        <link>{{ blog["baseurl"] + "/post/" + post["slug"] }}</link>
        <description>{{ post["description"] }}</description>
        <comments>{{ blog["baseurl"] + "/post/" + post["slug"] }}#comments</comments>
        {% for tag in post["tags"] %}
        <category domain="{{ blog["baseurl"] + "/tag/" + tag["slug"] }}">{{ tag["slug"] }}</category>
        {% end %}
        <pubDate>{{ post["pub_date"] }}</pubDate>
        <dc:creator>{{ post["author"] }}</dc:creator>
        <guid isPermaLink="false">{{ post["order_number"] }} at {{ blog["baseurl"] }}</guid>
    </item>
    {% end %}
</channel>
</rss>
